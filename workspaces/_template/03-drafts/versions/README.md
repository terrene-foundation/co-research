# Draft Snapshots

Draft snapshots go here. Save a snapshot before major revisions to maintain a version history.

Naming convention: `v[N]-[date]-[description].md` (e.g., `v1-2026-03-15-initial-structure.md`)

Each snapshot should be a complete, self-contained version of the paper at that point in time.
