# Workspaces

Each subdirectory is a research project workspace. Create a new workspace by copying `_template/`:

```bash
cp -r workspaces/_template workspaces/my-paper
```

Then add your paper brief to `my-paper/briefs/`.
